apiVersion: v1
Kind: pod
metadata:
   name: myapp
   labels:
     name: myapp
     image: <Image>
     resources:
       limited:
       memory: "128Mi"
       cpu: "500m"
    ports:
      - containerPort: <Port>

